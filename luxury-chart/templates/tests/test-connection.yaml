apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "luxury-chart.fullname" . }}-test-connection"
  labels:
    {{- include "luxury-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "luxury-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
